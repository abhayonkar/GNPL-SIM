# CGD IDS ML Pipeline — Python Dependencies
# Install: pip install -r requirements.txt
#
# Tested with Python 3.10 / 3.11

# ── Core ML ──────────────────────────────────────────────────────────────
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
joblib>=1.3

# ── Gradient boosting ────────────────────────────────────────────────────
xgboost>=1.7

# ── Explainability ───────────────────────────────────────────────────────
shap>=0.44

# ── Plotting ─────────────────────────────────────────────────────────────
matplotlib>=3.7

# ── Deep learning (temporal + graph models) ──────────────────────────────
torch>=2.1           # LSTM-AE, GRU, Hybrid fusion
torch-geometric>=2.4 # GDN / GAT graph models

# ── Gateway / protocol ───────────────────────────────────────────────────
pymodbus>=3.5
pyyaml>=6.0

# ── Utilities ─────────────────────────────────────────────────────────────
tqdm>=4.65           # progress bars during training
